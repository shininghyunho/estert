spring:
  profiles:
    active: local
    group:
      local:
        - local-db
        - local-scheduling
      dev:
        - dev-db
    # common
    include:
      - kakao
      - molit
      - vworld